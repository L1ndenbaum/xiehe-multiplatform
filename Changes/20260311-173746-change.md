# UI Refactor Batch 2: patient

## 变更范围
- 重构 `screens/patient`：迁移 `PatientsScreen`、`PatientDetailScreen`、`PatientFormScreen`、`PatientEditScreen`。
- 重构 `viewmodel/patient`：迁移 `PatientsViewModel`、`PatientDetailViewModel`、`PatientFormViewModel`、`PatientEditViewModel`、`PatientIdGenerator`。
- 更新 `App.kt` 与患者域页面的 import 到新 package。
- 修正迁移后患者筛选枚举与患者 ID 生成工具的包引用，使重构后的源码仍按原逻辑编译。

## 兼容处理
- 继续保留第 1 批引入的共享桥接文件，保证未迁移领域仍可引用旧共享包路径。
- 本批未新增新的过渡桥接，患者域直接切换到 `ui.screens.patient` 与 `ui.viewmodel.patient`。

## 验证结果
- 执行：`./gradlew.bat :composeApp:compileKotlinJvm`
- 结果：通过。
- 执行：`./gradlew.bat :androidApp:assembleDebug`
- 结果：通过。

## 说明
- 本批不改动患者相关业务逻辑或界面行为，只调整目录、package、import 和迁移后的引用修正。
