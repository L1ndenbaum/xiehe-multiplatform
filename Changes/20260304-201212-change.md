# Milestone C Change Log

## 变更范围
- 完成认证与个人中心入口相关页面 UI 重构：登录、注册、个人中心、消息中心。
- 统一页面为 NewDesign 对应的渐变背景、卡片化信息层、强化操作按钮样式。

## 涉及文件
- composeApp/src/commonMain/kotlin/com/xiehe/spine/ui/screens/LoginScreen.kt
- composeApp/src/commonMain/kotlin/com/xiehe/spine/ui/screens/RegisterScreen.kt
- composeApp/src/commonMain/kotlin/com/xiehe/spine/ui/screens/ProfileScreen.kt
- composeApp/src/commonMain/kotlin/com/xiehe/spine/ui/screens/MessagesScreen.kt

## 保留的业务逻辑
- 登录页继续通过 `LoginViewModel` 状态与 `onLogin/onHealthCheck/onOpenRegister` 回调驱动。
- 注册页继续调用 `RegisterViewModel.submit(authRepository, onBackToLogin)`。
- 个人中心保留原有菜单回调与退出登录回调。
- 消息中心保留原有首次加载与 60 秒轮询刷新机制。

## 验证结果
- 执行：`./gradlew :composeApp:compileKotlinJvm -q`
- 结果：构建环境错误 `What went wrong: 25.0.2`，未进入有效 Kotlin 编译阶段。

## 风险点
- 新消息卡片新增 `messageType` 标签视觉展示，数据为空时自动降级。
- 当前仅完成页面 UI 重构，后续仍需继续推进主业务页与 Overlay 页面以避免风格不一致。
