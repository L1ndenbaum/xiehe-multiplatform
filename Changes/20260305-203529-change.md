# DatePicker iOS 编译修复

## 变更范围
- 修复 `commonMain` 日期格式化在 iOS 目标下的编译错误。

## 涉及文件
- composeApp/src/commonMain/kotlin/com/xiehe/spine/ui/components/picker/DatePickerField.kt

## 保留的业务逻辑
- 仅替换日期字符串拼接实现，不改变日期选择器的任何交互、取值或回调逻辑。

## 验证结果
- 根据报错定位修复：移除 `"%04d-%02d-%02d".format(...)`，改为跨平台 `padStart`。
- 当前机器 Gradle 编译仍可能被既有环境问题（`25.0.2`）阻断，未能在本机完成完整构建验证。

## 风险点
- 风险极低：仅日期字符串格式化函数内部实现变更，输出格式与原来一致（`YYYY-MM-DD`）。
