# UI Refactor Batch 5: cleanup

## 变更范围
- 删除过渡期桥接文件：
  - `ui/components/LegacySharedBridge.kt`
  - `ui/viewmodel/LegacyViewModelBridge.kt`
- 复核 `composeApp/src/*` 下 UI 目录的 package 声明，确认不再存在 `com.xiehe.spine.ui.components`、`com.xiehe.spine.ui.screens`、`com.xiehe.spine.ui.viewmodel` 这类旧根包声明。

## 验证结果
- 执行：`./gradlew.bat :composeApp:compileKotlinJvm`
- 结果：通过。
- 执行：`./gradlew.bat :androidApp:assembleDebug`
- 结果：通过。
- 执行：`rg -n "^package com\.xiehe\.spine\.ui\.(screens|viewmodel|components)$" composeApp/src`
- 结果：无匹配，旧根包声明已清空。

## 说明
- 由于前四批迁移后已不存在旧根包 import，cleanup 才删除桥接文件；本批不包含任何业务逻辑或 UI 行为调整。
